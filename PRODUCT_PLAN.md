# Go 语言复刻 Nexus OSS 的产品功能规划（增强版）

## 1. 产品定位

目标是构建一个 **更轻量、更灵活、更易扩展** 的制品仓库平台（以下简称 "G-Nexus"），在兼容 Nexus OSS 核心能力基础上，重点优化以下方向：

- 云原生友好（Go + React，低资源占用，便于容器化与分布式部署）
- 多租户与企业级接入能力（OAuth2、仓库自定义域名）
- 模块化插件体系（后端插件 + 前端扩展）
- 更现代的自动化任务系统（策略化、声明式、可观测）
- 分布式主动同步 + 边缘 Agent 预热（镜像、包管理资源）
- 代理仓库能力增强（缓存模式 + 纯穿透模式）
- 生态资源改写能力（如 Helm `index.yaml` 动态改写）

---

## 2. 核心用户与场景

### 2.1 核心用户

- 平台工程团队（统一管理制品与镜像）
- 研发团队（依赖拉取、发布、版本治理）
- 运维/安全团队（访问控制、审计、同步策略）

### 2.2 关键业务场景

1. 企业内网统一制品入口（Maven/NPM/PyPI/Docker/Helm）
2. 多 IDC/多地域部署下的低延迟拉取
3. 开发/测试环境快速预热（镜像和依赖包）
4. 第三方源代理与隔离控制（缓存或穿透）
5. 不同仓库类型在 UI 上展示差异化功能（插件化页面）

---

## 3. 功能模块规划（MVP → 增强）

## 3.1 仓库管理（基础能力）

### MVP
- 仓库类型：Hosted / Proxy / Group
- 支持格式：Docker、Helm、Maven（首批），后续扩展 NPM/PyPI 等
- 仓库粒度配置：权限、保留策略、清理策略、配额
- 审计日志与基础统计

### 增强
- 仓库级自定义域名绑定（含证书配置与 SNI 路由）
- 访问策略模板（按项目、环境、租户复用）
- 高级查询（按坐标、标签、构建信息）

## 3.2 认证与授权

### MVP
- 本地账号体系 + Token
- RBAC（系统角色 + 仓库角色）

### 增强
- OAuth2 / OIDC 登录（GitHub、GitLab、企业 IdP）
- 细粒度权限（仓库、路径、操作维度）
- 临时访问凭证（短期 token + 审批流扩展点）

## 3.3 自动化任务系统（更简易、更灵活）

### MVP
- 任务模型：定时任务、事件触发任务（上传后、同步后）
- 任务类型：清理、同步、索引重建、Webhook 通知
- 可视化任务执行历史与失败重试

### 增强
- 声明式任务编排（DAG）
- 策略引擎（按仓库类型、路径规则、元数据自动触发）
- 插件任务类型（第三方自定义任务）

## 3.4 分布式与同步

### MVP
- 多节点读写分离部署
- 元数据存储与对象存储解耦
- 基础被动同步（按请求触发）

### 增强
- 主动同步（计划任务 + 事件驱动，支持跨地域）
- 同步拓扑管理（中心-边缘、级联）
- 冲突检测与回滚策略

## 3.5 Agent 能力（轻量预热）

### MVP
- 轻量 Agent 注册与心跳
- 指定资源预热（Docker 镜像、Helm 包）

### 增强
- Agent 任务编排与批次发布
- 分区域就近预热策略
- 预热命中率与成本可视化

## 3.6 代理模式增强

### MVP
- 缓存代理（Cache-Through）
- 缓存生命周期与失效策略

### 增强
- 纯穿透模式（Pass-Through，不落盘）
- 混合模式（按路径/命名空间动态选择缓存或穿透）
- 上游健康探测与自动切换

## 3.7 Helm 能力增强

### MVP
- Helm 仓库托管与代理
- chart 上传、索引维护

### 增强
- `index.yaml` 动态改写（URL、签名、元数据增强）
- 多域名/多租户场景下的索引视图
- 签名验证与供应链安全策略

## 3.8 前端扩展机制（React + shadcn/ui）

### MVP
- 通用仓库浏览界面（列表、详情、版本、下载、元数据）
- React + shadcn/ui 组件体系

### 增强
- 仓库类型插件化视图（例如 Docker 展示镜像层、Helm 展示 chart 依赖）
- 前端插件注册协议（菜单、页面块、详情面板扩展）
- 可配置 UI schema（低代码布局）

---

## 4. 架构优化设计（相对 Nexus OSS）

## 4.1 整体架构分层

- **Gateway/API 层**：统一认证、限流、路由（支持自定义域名）
- **Repository Core 层**：仓库协议处理、元数据模型、读写策略
- **Storage 抽象层**：本地盘、S3、OSS、COS 等
- **Sync & Task 层**：主动同步、任务编排、事件总线
- **Plugin Runtime 层**：后端插件 + 前端插件
- **Agent Control 层**：边缘 agent 调度与预热

## 4.2 技术选型建议

- 后端：Go（Gin/Fiber + gRPC + OpenTelemetry）
- 存储：PostgreSQL（元数据）+ 对象存储（制品）+ Redis（缓存/队列）
- 消息：NATS / Kafka（二选一，先 NATS 轻量起步）
- 前端：React + TypeScript + shadcn/ui + TanStack Query
- 部署：Kubernetes + Helm（支持单机到分布式平滑升级）

## 4.3 低资源与高性能策略

- 传输层流式处理，避免大文件内存拷贝
- 元数据热路径缓存（读多写少）
- 分块上传与断点续传
- 插件沙箱隔离，避免核心路径被拖慢

---

## 5. 插件化设计（模块化核心）

## 5.1 后端插件点

- 仓库格式适配器（Format Adapter）
- 鉴权扩展（Auth Provider）
- 任务执行器（Task Executor）
- 索引改写器（如 Helm index rewriter）
- 同步策略器（Sync Strategy）

## 5.2 前端插件点

- 路由页签扩展
- 仓库详情面板扩展
- 列表列定义扩展
- 操作菜单扩展（批量动作、自定义按钮）

## 5.3 插件治理

- 插件签名与来源校验
- 插件权限声明（可访问 API 范围）
- 版本兼容矩阵与热升级策略

---

## 6. 版本路线图（建议）

## v0.1（MVP，8~12 周）
- Hosted/Proxy/Group 基础能力
- Docker + Helm + Maven
- 本地认证 + RBAC
- 通用仓库 UI（React + shadcn/ui）
- 基础任务系统（定时/事件）

## v0.2（增强，6~8 周）
- OAuth2/OIDC
- 自定义域名
- 主动同步（基础版）
- Agent 预热（Docker/Helm）
- Helm `index.yaml` 改写（第一版）

## v0.3（扩展，8~12 周）
- 前后端插件机制正式化
- 纯穿透 + 混合代理模式
- 高级策略引擎 + DAG 编排
- 多地域同步拓扑与治理

---

## 7. 非功能性目标（SLO 建议）

- 可用性：核心 API 99.9%+
- 性能：常见元数据查询 P95 < 200ms
- 大文件上传下载：支持并发分块、失败重试
- 安全：审计全链路、最小权限、密钥轮转
- 可观测：日志、指标、链路追踪默认开启

---

## 8. 实施优先级建议

1. 先打通核心闭环（仓库读写 + 权限 + UI）
2. 优先实现 Docker/Helm（覆盖率高）
3. 再做 OAuth2、自定义域名（企业落地关键）
4. 最后做插件和高级分布式（形成差异化）

这个顺序能在早期快速提供可用产品，同时保留中后期架构演进空间。
